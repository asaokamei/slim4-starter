{% extends('layouts/layout.html.twig') %}

{% block contents %}

<h1>Form Samples</h1>

<p>This is form sample...</p>

{{ form }}
<input type="hidden" name="_csrf_name" value="{{ csrf[0] }}">
<input type="hidden" name="_csrf_value" value="{{ csrf[1] }}">

{% if data is defined %}
    <h2>form submitted...</h2>
    {% for key, val in data %}
        {{ key }}: {{ val }}<br>
    {% endfor %}
{% endif %}

{% endblock %}